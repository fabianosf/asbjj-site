server {
    listen 80;
    listen [::]:80;
    server_name asbjj.com.br www.asbjj.com.br;

    root /var/www/asbjj.com.br/dist;
    index index.html;

    # Logs
    access_log /var/log/nginx/asbjj-access.log;
    error_log /var/log/nginx/asbjj-error.log;

    # Configuração para SPA (Single Page Application)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache para assets estáticos
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Segurança
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Tamanho máximo de upload
    client_max_body_size 10M;
}

# Configuração HTTPS (após configurar SSL com Certbot)
# Descomente e ajuste após executar: sudo certbot --nginx -d asbjj.com.br -d www.asbjj.com.br
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name asbjj.com.br www.asbjj.com.br;
#
#     ssl_certificate /etc/letsencrypt/live/asbjj.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/asbjj.com.br/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /var/www/asbjj.com.br/dist;
#     index index.html;
#
#     access_log /var/log/nginx/asbjj-access.log;
#     error_log /var/log/nginx/asbjj-error.log;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     client_max_body_size 10M;
# }

